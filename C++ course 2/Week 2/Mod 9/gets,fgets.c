#include <stdio.h>
#include <string.h>

int main ()
{
    // int n;
    char a[50];
    // fgets (a,20,stdin);

    // a[15]='\0';
    // printf("%s",a);

    //  scanf ("%s",&a);
    // //  int st = strlen(a);   (we could use strlen function to count strings or build logic to count length)
    //  printf("%d",st);

         
     return 0;
}

