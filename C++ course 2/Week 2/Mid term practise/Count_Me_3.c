#include <stdio.h>
#include <string.h>
int main ()
{
         int n;
         scanf ("%d",&n);

         char s[10000]; `1
         int length=strlen(s);

         for (int i=0;i<length;i++)
         {
          scanf("%s",s[i]);
          
         }

         for (int i=0;i<length;i++)
         {
            printf("%s\n",s[i]);
         }
     return 0;
}

