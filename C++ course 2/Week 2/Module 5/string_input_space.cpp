#include <bits/stdc++.h>
using namespace std;

int main()
{
      int x;
      string s; // c++ er char data type e cin.getline use kore input nea hoy
      //cin.getline() // string jehetu dynamic object use kore tai built in function getline(cin,stirng_name) use korte hbe string data type  input nite ar char data type cin.getline normal c++ er moto,se auto size soho input niye ney but jamela hochhe er age jdi enter pore then se just enter niye ney baki character ney na, string er por enter porle somossa nei , eta c,c++,string sob jaygay e somossa
      cin>>x;
    //   getchar();
      getline(cin,s); 
    // cin>>s;
    //   cin>>x;
      cout<<x<<endl<<s;
          
    return 0;
}
