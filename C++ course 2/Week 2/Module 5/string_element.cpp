#include <bits/stdc++.h>
using namespace std;

int main()
{
        //  string s="loveandwar";
        //  cout<<s[0]<<endl;
        //  cout<<s.at(1)<<endl;
        //  cout<<s.back()<<endl;// last index er value print korbe 
        
        //  cout<<s[s.size()-1]<<endl; // suppor string a="abc" so size will be 3 and index will be 0,1,2 so s.back() hocche size-1 ba last index  print kre
        //  cout<<s.front()<<endl;
        //  cout<<s[0]<<endl;

         /*so jehetu era same type kaj kore jeta easier likhte setai use korbo amra
                s[0]/s.front() 
                s.back()/s.size-1(last index)
                s[]/s.at() */

                //string addition

                // string s="lovehurt";
                // string t="hurt";
                // // s+=t;
                // s.append(t);
                // s.push_back('A');
                // s.pop_back();// vitore kichu dite hobe na just last char delete kore dibe

                // cout<<s<<endl;

                // s.erase(4);  //2 ta parameter ney and 2 ta parameter e 2 ta kaj kore,, just 1st parameter dile oita index hieseb ney and oi index soho porer gulo dlt kore dey like s.resize()
               // s.erase(4,2);            //2 ta parameter dile 1st parameter er index theke porer parameter porjonto delete kore dibe baki gulo thkbe ager motoi.
                // s.replace(4,2,"S"); // forcefully oi index er jaygay se dokhol korbe baki ei index er gulo right e shore jabe
                // s.insert(0,"X"); same almost like s.replace ()
                // cout<<s;
                
                
            


                
    return 0;
}
