#include <bits/stdc++.h>
using namespace std;

int main()
{       
    //  vector <data_type> vector_name;
       //it is constructor of vector class(as constractor and class name are same) with 0 elements

        
        
         vector<int>v;   //constractor type 1 
         vector <int>v(N); // constructor type 2 ; here we declare the size of the vector and all it take n size vector with garbage values.
         vector <int > (N,V);  // constructor type 3; here we decalare the size along with the vector v, So all size will fulfilled with values of v vairables.
         vector <int > v(v2) ;  // contstructor type 4; here all values of v2 vector are copied in v.
         vector <int > v(A,A+N);  // constructor type 5; all values of an array A are copied from frist to last in this vector v.





    return 0;
}
